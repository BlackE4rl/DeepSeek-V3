# Core dependencies for the backtest engine, analysis and tests.
# Deliberately contains no networking library: the analytical core must be
# runnable and testable in a fully offline environment.
numpy==2.1.3
pandas==2.2.3
matplotlib==3.9.2
PyYAML>=6.0,<7
pytest==8.3.3
